# 配置端口
server:
  port: 80

# 配置数据库和连接池
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mldn?serverTimezone=UTC
# 配置MP
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tbl_
